#include <iostream>
#include <vector>
using namespace std;

int main(){
    vector<double> v = {4, 8, -3, 10, 2};
    double mini = v[0];

    for(double x : v){
        if(x < mini) mini = x;
    }

    cout << "Minimo = " << mini;
}
