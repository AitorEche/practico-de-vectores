#include <iostream>
#include <vector>
using namespace std;

int main(){
    int n; 
    cin >> n;

    vector<int> F(n);
    F[0] = 0;
    if(n > 1) F[1] = 1;

    for(int i=2; i<n; i++)
        F[i] = F[i-1] + F[i-2];

    for(int x : F) cout << x << " ";
}
