#include <iostream>
#include <vector>
using namespace std;

int main(){
    vector<int> A = {5,1,3,2,4};

    for(int i=0; i<A.size(); i++){
        for(int j=0; j<A.size()-1; j++){
            if(A[j] > A[j+1]) swap(A[j], A[j+1]);
        }
    }

    for(int x : A) cout << x << " ";
}
