#include <iostream>
#include <vector>
using namespace std;

int main(){
    vector<double> v = {4, 8, -3, 10};
    double suma = 0;

    for(double x : v) suma += x;

    cout << "Promedio = " << suma / v.size();
}
